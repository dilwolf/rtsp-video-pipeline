# For CUDA, install torch from the matching wheel index BEFORE this file:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# Otherwise pip resolves the CPU build and --device 0 will not work.
ultralytics>=8.3
supervision>=0.26
opencv-python>=4.10
numpy>=1.26

# tracking. ultralytics ships ByteTrack; lap is its assignment solver
lap>=0.5

# mlops
mlflow>=2.16
dvc>=3.55

pytest>=8.0
